#include <iostream>
#include <string>
using namespace std;

string s[10];
int len[10] = {0,};
int alpha[10] = {0,};

int main(){
    int N;
    cin >> N;


    for (int i=0;i<N;i++){
        cin >> s[i];
        len[i] = s->length();
        for (int i=0;i<len[i];i++){
            if (!visited[s[i]]){

            }
        }

    }

}